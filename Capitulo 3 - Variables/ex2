duplicar(4)
double duplicar(double a) {
    var foo = a * 2;
    return foo;
}
duplicar(5)
int mod(int p, int q) {
    var d = p / q;
    var r = d * q;
    var m = p - r;
    return m;
}
mod (3,2)
mod(4,2)